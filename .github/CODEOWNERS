* @branchLeft/branchleft-admin

# GitHub reports no CODEOWNERS errors as the only signal that an entry
# resolved: an owner without write access is dropped silently, with no error
# and no warning on the pull request. "The file says so" is not evidence.
#
# The shared line above stays. SYNC-1 is `contains`, not `identical`: a repo
# may add to this file and may never drop a shared line, and rewriting that
# line in place is dropping it.
* @reviewer-agent-branchLeft @branchLeft/branchleft-admin
